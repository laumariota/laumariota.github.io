#FUNCION SIN PARAMETROS
# 2. diseñe una app con una funcion que calcule el area del triangulo y sea llamda por un algoritmo
def areas():
    area=b*(a/2)
    print("el area del triangulo es: ",area)

b=float(input("Digite la base del triangulo: "))
a=float(input("Digite la altura de un triangulo: "))

areas() #llamando a la funcion 
